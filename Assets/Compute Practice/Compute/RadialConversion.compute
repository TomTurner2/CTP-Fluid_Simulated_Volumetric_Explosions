#pragma kernel CSMain

RWTexture2D<float4> Result;
float dt;

[numthreads(8,8,1)]
void CSMain (uint3 id : SV_DispatchThreadID)
{
	float r = sin(dt * sin(id.y) + cos(id.x) * 2);
	r = clamp(r, 0, 1);

	Result[id.xy] = float4(r, r, r, r);
}
